# MVP-1 Kit

This `kit/` directory is the minimal MVP-1 bootstrap skeleton created by StepPack `S1`.

Contents:
- `templates/` placeholder assets for future control-plane bootstrap templates.

Scope note:
- This file is intentionally small to satisfy the MVP-1 S1 acceptance checks.
