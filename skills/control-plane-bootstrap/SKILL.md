---
name: control-plane-bootstrap
description: Install a minimal AI Coding Control Plane bootstrap kit (Contract + Runtime + Evidence + Harness + Drift Governor) into a target repository. Use when a user asks to initialize or bootstrap the control plane kit and starter assets.
---

# Control Plane Bootstrap

Use this skill when the user wants a minimal bootstrap kit scaffold for control-plane execution, templates, and starter workflow files.

## Purpose

Create a small, auditable starting structure for MVP-1 without changing dependencies or lockfiles.

## Outputs

- `kit/` skeleton and template placeholders
- bootstrap skill assets placeholders
- a minimal CI workflow skeleton (when requested by the task)
