# EVO Index

This is the starting index for EVO notes and lessons captured during controller/worker iterations.

## Entries

- `lessons.log` for short append-only learning notes

## Run/Step Entry Format

Keep `lessons.log` append-only and add one non-comment line per notable run/step outcome.

Suggested fields:

- timestamp
- run_id
- step_id
- lesson summary
